<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TicketFlowmobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- La franja inferior (sistema de gestos) debe heredar el fondo de la app
             y no dibujar cajas blancas/negras contrastantes. -->
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.TicketFlowmobile" parent="Base.Theme.TicketFlowmobile" />
</resources>